<%= form_with(model: @review, local: true) do |f| %>

  <div class="form-group">
    <%= f.label :dm_id %>

    <%= f.collection_select :dm_id, Dungeonmaster.all, :id, :to_s, {}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :rating %>

    <%= f.text_field :rating, class: "form-control" %>
  </div>

  <%= f.hidden_field :reviewer_id, value: @player.id %>

  <div class="form-group">
    <%= f.label :review %>

    <%= f.text_field :review, class: "form-control" %>
  </div>

  <%= f.submit "Create review", class: "btn btn-block btn-outline-secondary" %>
<% end %>
